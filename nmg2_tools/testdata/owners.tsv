# Owner declarations.
#
# Format: repository<TAB>path<TAB>owner-task
#
# A row declares that when more than one task's Files: line claims the
# same (repository, path) pair, the named owner-task is the task that
# actually owns the write to that path. This file is a starter; the
# tests drive their own synthetic rows and do not read this file.

gearmulator	g2Lib/g2_patch.cpp	T0-1
