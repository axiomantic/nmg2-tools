"""A reader with half a record.

WHAT THIS FILE IS, because the licence makes it matter.

This repository is MIT. That is the whole record: it never says whether any
line of another implementation was copied.
"""


def read(data: bytes) -> int:
    return len(data)
