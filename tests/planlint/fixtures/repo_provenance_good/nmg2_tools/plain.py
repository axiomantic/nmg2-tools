"""A module that touches no external format and names no licence.

It carries no provenance record and the lint asks for none, because nothing
about it says it restates somebody else's format.
"""


def add(left: int, right: int) -> int:
    return left + right
